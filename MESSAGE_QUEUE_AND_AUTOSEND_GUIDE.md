# 消息队列和主动消息配置指南

## 功能1：消息队列合并（已实现）

### 功能说明
- **10秒内的多条消息会自动合并处理**
- 图片和文本消息如果在10秒内发送，会被合并成一条消息
- AI会同时看到所有消息内容，做出更完整的回复

### 工作原理
```
用户：消息1
      [等待中...]
用户：消息2（5秒后）
      [等待中...]
用户：消息3（8秒后）
      [10秒到期，触发处理]
AI：收到合并消息："消息1；消息2；消息3"
AI：给出一次性回复
```

### 配置方法

在 `data/config/config.json` 中：

```json
{
  "behavior_settings": {
    "settings": {
      "message_queue": {
        "timeout": {
          "value": 10,
          "type": "number",
          "description": "消息队列等待时间（秒）",
          "min": 0,
          "max": 20
        }
      }
    }
  }
}
```

**参数说明**：
- `timeout`: 消息合并等待时间（秒）
  - 默认：10秒
  - 范围：0-20秒
  - 推荐：10-15秒

### 使用示例

**场景1：图片+文字**
```
用户：[发送图片]
用户：这是什么？（2秒后）
      [8秒后合并处理]
AI：收到 "用户发送了一张照片，照片的内容是：一碗拌面...；这是什么？"
AI：这是一碗葱油拌面！看起来很美味~
```

**场景2：连续消息**
```
用户：你知道吗
用户：我今天
用户：遇到了一件很有趣的事
      [10秒后合并]
AI：收到 "你知道吗；我今天；遇到了一件很有趣的事"
AI：哦？快告诉我！是什么有趣的事？
```

---

## 功能2：主动消息（已修复）

### 功能说明
- Bot会在指定的时间间隔内**随机主动**向用户发送消息
- 可以设置安静时间（不打扰时段）
- 支持自定义消息内容或让AI自由发挥

### 配置方法

#### 1. 基础配置

在 `data/config/config.json` 中：

```json
{
  "user_settings": {
    "settings": {
      "listen_list": {
        "value": [
          "1234567890",
          "另一个QQ号"
        ],
        "type": "array",
        "description": "要接收主动消息的QQ号列表"
      }
    }
  },
  "behavior_settings": {
    "settings": {
      "auto_message": {
        "enabled": {
          "value": true,    // ← 设为true启用
          "type": "boolean"
        },
        "content": {
          "value": "【系统指令】请你作为雪乃，在QQ上发消息说你现在想说的话",
          "type": "string"
        },
        "countdown": {
          "min_hours": {
            "value": 0.5,    // 最短0.5小时
            "type": "number"
          },
          "max_hours": {
            "value": 3,      // 最长3小时
            "type": "number"
          }
        }
      },
      "quiet_time": {
        "start": {
          "value": "23:00"   // 安静时间开始
        },
        "end": {
          "value": "07:00"   // 安静时间结束
        }
      }
    }
  }
}
```

#### 2. 参数说明

**listen_list**（监听列表）：
- 填写QQ号字符串
- 多个用户时，Bot会随机选择一个发送
- 例：`["1234567890", "0987654321"]`

**enabled**（启用开关）：
- `true`: 启用主动消息
- `false`: 禁用主动消息

**content**（消息内容）：
- 可以自定义具体消息
- 也可以使用系统指令让AI自由发挥
- 留空则使用默认指令

**countdown**（时间间隔）：
- `min_hours`: 最短间隔（小时）
- `max_hours`: 最长间隔（小时）
- Bot会在这个范围内随机选择下次发送时间

**quiet_time**（安静时间）：
- `start`: 开始时间（24小时制）
- `end`: 结束时间（24小时制）
- 在此时段内不会发送主动消息

### 使用方法

#### 启动方式1：通过配置文件

1. 编辑 `data/config/config.json`
2. 设置 `enabled: true`
3. 重启Bot

#### 启动方式2：通过命令（推荐）

在私聊中发送：
- `/start_autosend` 或 `启动自动消息` - 启动功能
- `/stop_autosend` 或 `停止自动消息` - 停止功能
- `/autosend_status` 或 `自动消息状态` - 查看当前状态

### 工作流程示例

```
Bot启动
  ↓
读取配置：enabled=true, 间隔=0.5-3小时
  ↓
随机选择下次发送时间：1.5小时后
  ↓
等待1.5小时...
  ↓
检查：当前时间02:00，不在安静时间(23:00-07:00)内
  ↓ 在安静时间，跳过
等待到07:00后再尝试
  ↓
随机选择用户：1234567890
  ↓
发送主动消息
  ↓
AI生成回复："早上好呀~昨晚睡得好吗？"
  ↓
重新随机选择下次间隔：2.3小时后
  ↓
循环...
```

---

## 常见问题

### Q1: 消息队列不工作？
**A:** 检查配置文件中的 `timeout` 值是否正确设置。重启Bot让配置生效。

### Q2: 主动消息不发送？
**A:** 检查以下几点：
1. `enabled` 是否为 `true`
2. `listen_list` 是否填写了QQ号
3. 是否在安静时间内
4. 查看日志中的 `[AutoSend]` 信息

### Q3: 如何查看主动消息状态？
**A:** 发送命令 `/autosend_status` 查看详细状态。

### Q4: 可以针对不同用户设置不同的发送频率吗？
**A:** 当前版本暂不支持，所有用户使用相同的时间间隔配置。

### Q5: 消息队列会影响命令吗？
**A:** 不会。命令（如 `/help`）会立即处理，不进入队列。

---

## 测试方法

### 测试消息队列：
1. 快速连续发送3条消息（间隔<10秒）
2. 等待10秒
3. 观察Bot是否给出一次性回复
4. 检查日志中的 `[MessageQueue]` 信息

### 测试主动消息：
1. 设置很短的间隔（如 0.05-0.1小时，即3-6分钟）
2. 启用主动消息：`/start_autosend`
3. 等待并观察
4. 检查日志中的 `[AutoSend]` 信息

---

## 故障排查

### 查看日志
```bash
# 实时查看日志
tail -f logs/qq_bot_*.log | grep -E "\[MessageQueue\]|\[AutoSend\]"
```

### 常见日志信息

**消息队列**：
```
[MessageQueue] 创建新队列 - 用户: 1234567890
[MessageQueue] 追加消息 - 用户: 1234567890, 当前消息数: 2
[MessageQueue] 处理队列 - 消息数: 2, 合并后: 消息1；消息2
```

**主动消息**：
```
[AutoSend] 配置加载成功 - 启用: True, 监听用户数: 1
[AutoSend] 启动自动消息定时任务，首次执行间隔: 1.50小时
[AutoSend] 成功向用户 1234567890 发送主动消息
[AutoSend] 下次自动消息将在 2.30小时 后发送
```

---

## 更新日志

**2025-10-28**
- ✅ 实现消息队列合并功能
- ✅ 修复主动消息配置读取问题
- ✅ 添加 `enabled` 开关
- ✅ 优化配置结构
- ✅ 添加详细日志输出

---

**需要帮助？** 查看日志文件或在配置文件中调整参数后重启Bot。