
# 智能情境表情系统使用指南

## 📖 概述

智能情境表情系统是一个基于AI的自动表情包发送功能，它能够：
- 🎯 **自动识别对话情境**：分析用户消息和Bot回复中的情境（天气、食物、日常活动等）
- 🧠 **智能判断发送时机**：区分真实情绪和打情骂俏，避免在严肃场合发送表情
- 🔍 **全局表情检索**：不受文件夹限制，从所有表情中智能匹配最合适的
- 📁 **灵活文件管理**：支持自由组织表情文件夹结构，文件名即关键词
- 🎲 **频率控制**：20%概率发送 + 2分钟冷却，避免过于频繁

## 🎨 系统特性

### 1. 双模式表情系统

#### **情境表情模式**（新增）
基于对话场景自动发送，例如：
- Bot说："我这边下雨了" → 自动发送下雨表情包
- Bot说："吃完饭了" → 自动发送吃饭表情包
- Bot说："在看书呢" → 自动发送看书表情包

#### **情感表情模式**（原有）
通过`[happy]`、`[sad]`等标签触发，由AI判断真实情感：
- 轻松愉快的对话 → 发送表情
- 真的生气/严肃话题 → **不发送**表情
- 打情骂俏场景 → 可以发送angry/sad表情

### 2. 扁平化全局检索

**文件组织方式**：
```
data/avatars/角色名/emoji/
├── 天气/
│   ├── 下雨1.gif
│   ├── 下雨2.gif
│   └── 晴天.gif
├── 食物/
│   ├── 吃饭1.gif
│   └── 喝水.gif
└── 日常/
    ├── 看书.gif
    └── 睡觉.gif
```

**关键特性**：
- ✅ 文件夹只是为了方便管理，不影响检索
- ✅ 文件名即关键词（支持多关键词，如"下雨打伞.gif"）
- ✅ AI全局搜索，自动匹配最相关的表情
- ✅ 支持同义词映射（雨天=下雨，进食=吃饭）

### 3. 双向情境检测

系统同时分析：
- **用户消息**："外面下雨了" → 识别下雨情境
- **Bot回复**："我这边也下雨了" → 识别下雨情境

任一方提到情境，都会触发智能表情发送。

## ⚙️ 系统参数

### 发送频率控制

为了避免表情发送过于频繁，系统内置了双重控制机制：

1. **概率控制**：20%
   - 即使检测到合适的情境，也只有20%的概率真正发送
   - 保持表情的惊喜感和新鲜度

2. **冷却时间**：2分钟
   - 每个用户发送表情后，需要等待2分钟才能再次发送
   - 避免短时间内重复发送

**实际效果**：
- 平均每5次合适情境中发送1次
- 同一用户每小时最多发送30次（理论值）
- 实际使用中，大约每10-15分钟发送1次

### 修改参数

如需调整频率，可修改 [`plugins/chat.py`](plugins/chat.py:103) 中的参数：

```python
self.EMOJI_PROBABILITY = 0.2  # 发送概率（0.2 = 20%）
self.EMOJI_COOLDOWN = 120     # 冷却时间（秒）
```

**推荐配置**：
- 更保守：10%概率 + 5分钟冷却
- 当前设置：20%概率 + 2分钟冷却 ⭐
- 更活跃：30%概率 + 1分钟冷却
- 无限制：100%概率 + 0秒冷却（不推荐）

## 🚀 快速开始

### 1. 准备表情包

在角色目录下创建emoji文件夹：
```bash
data/avatars/你的角色名/emoji/
```

添加表情文件，**文件名要包含关键词**：
```
下雨.gif
晴天.png
吃饭.jpg
开心1.gif
开心2.gif
生气.gif
委屈.gif
```

### 2. 自动构建索引

系统会在首次运行时自动扫描所有表情文件，生成索引：
```json
// emoji_index.json (自动生成)
{
  "emojis": [
    {
      "filename": "下雨.gif",
      "path": "data/avatars/角色名/emoji/天气/下雨.gif",
      "keywords": ["下雨"],
      "category": "天气",
      "type": "context"
    }
  ],
  "last_update": "2025-10-28 12:00:00"
}
```

### 3. 开始使用

无需任何配置，系统会自动工作：

**对话示例1：天气情境**
```
用户: 今天天气怎么样？
Bot: 我这边下雨了呢
[系统自动发送：下雨.gif]
```

**对话示例2：食物情境**
```
用户: 吃饭了吗？
Bot: 刚吃完，好饱~
[系统自动发送：吃饭.gif]
```

**对话示例3：情感判断**
```
用户: 你为什么不理我？
Bot: [angry] 因为你总是这样！
[系统判断：真的生气，不发送表情]
```

**对话示例4：打情骂俏**
```
用户: 你今天怎么这么可爱~
Bot: [angry] 讨厌啦，人家一直都很可爱好不好
[系统判断：打情骂俏，发送生气表情]
```

## 📋 支持的情境类型

### 天气类 (weather)
- 关键词：下雨、晴天、阴天、雪、雾、风、热、冷
- 示例：`下雨打伞.gif`、`晴天太阳.png`

### 食物类 (food)
- 关键词：吃饭、喝水、茶、咖啡、水果、零食
- 示例：`吃饭1.gif`、`喝咖啡.jpg`

### 日常活动类 (daily)
- 关键词：看书、睡觉、工作、学习、运动、玩游戏
- 示例：`看书.gif`、`睡觉Zzz.png`

### 特殊时刻类 (special)
- 关键词：生日、节日、周末、早安、晚安
- 示例：`生日快乐.gif`、`早安.jpg`

### 情感类 (emotion)
- 关键词：开心、生气、难过、委屈、害羞、惊讶
- 示例：`开心1.gif`、`生气.png`、`委屈.jpg`

## 🔧 高级配置

### 1. 命名规范建议

**推荐命名方式**：
```
✅ 下雨.gif              # 简单直接
✅ 下雨打伞.gif          # 多关键词
✅ 吃饭1.gif             # 编号区分
✅ 开心大笑.gif          # 情感+动作
✅ 生日蛋糕.gif          # 场景+物品
```

**不推荐命名**：
```
❌ IMG_001.gif          # 无意义
❌ emoji1.gif           # 无法识别
❌ 123456.png           # 纯数字
```

### 2. 同义词映射

系统内置同义词支持，会自动识别：
```python
"雨天" → "下雨"
"雨" → "下雨"
"下雪" → "雪"
"进食" → "吃饭"
"用餐" → "吃饭"
"饮水" → "喝水"
"阅读" → "看书"
"休息" → "睡觉"
```

### 3. 相关度评分机制

系统使用智能评分算法选择最佳表情：
```
完全匹配: +10分  # 关键词完全一致
部分匹配: +5分   # 关键词部分匹配
分类匹配: +3分   # 在同一分类中
```

**示例**：
对话中提到"下雨"：
- `下雨.gif` → 10分（完全匹配）
- `下雨打伞.gif` → 10分（完全匹配）
- `雨天.gif` → 5分（部分匹配）
- `晴天.gif` → 3分（天气分类）

系统会随机选择得分最高的表情。

### 4. 重建索引

如果添加了新的表情文件，可以删除索引文件让系统重建：
```bash
# 删除索引文件
rm data/avatars/角色名/emoji/emoji_index.json

# 重启Bot，系统会自动重建索引
```

## 🎯 情境识别规则

### 什么时候会发送表情？

✅ **会发送**：
- 轻松愉快的聊天氛围
- 日常生活话题
- 打情骂俏（即使是angry/sad情感）
- 分享日常（吃饭、天气、活动）

❌ **不会发送**：
- 真的生气或不开心
- 严肃话题（工作、学习压力）
- 悲伤情绪表达
- 批评或指责

### 情境识别示例

**示例1：下雨情境**
```
识别结果：
{
  "has_context": true,
  "context_type": "weather",
  "context_name": "rain",
  "keywords": ["下雨", "打伞"],
  "should_send_emoji": true,
  "confidence": 0.9
}
```

**示例2：真的生气**
```
识别结果：
{
  "has_context": false,
  "should_send_emoji": false,
  "reason": "用户表达了真实的不满情绪，不适合发送表情"
}
```

## 📊 日志和调试

### 查看日志

系统会输出详细日志：
```
[SmartEmoji] 检测到情境 - 类型:weather, 名称:rain, 置信度:0.9
[SmartEmoji] 选择表情: data/avatars/角色名/emoji/天气/下雨.gif
[SmartEmoji] 已发送情境表情
```

### 常见问题

**Q1: 为什么没有发送表情？**
- 检查是否有匹配的表情文件
- 查看日志中的情境识别结果
- 确认对话氛围是否适合发送表情

**Q2: 表情选择不准确？**
- 优化表情文件命名，使用更明确的关键词
- 检查`emoji_index.json`中的关键词提取是否正确
- 添加更多相关表情增加选择范围

**Q3: 如何禁用智能表情？**
- 删除或重命名emoji文件夹
- 或在代码中注释相关初始化代码

## 🔄 与原有表情系统的兼容

智能情境表情系统与原有的`[happy]`标签系统**并存**：

### 原有系统（保留）
```python
Bot: 今天真开心！[happy]
# 发送happy分类中的随机表情
```

### 新增系统（自动）
```python
Bot: 我这边下雨了
# 自动检测并发送下雨表情
```

### 同时使用
```python
Bot: 下雨天在家看书真舒服[happy]
# 1. 发送happy表情（标签触发）
# 2. 发送下雨/看书表情（情境触发）
```

## 🎓 最佳实践

### 